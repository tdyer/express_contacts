mixin login_status(user)
  if(user)
    p You are logged in as #{user.username}
      a(href="/auth/logout")  logout
  else
    p
      a(href="/auth/login") Please login
      |  or
      a(href="/auth/register")  register